{
  "name": "haushalt-mvcs",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k -n SERVER,CLIENT -c auto \"npm run -w server dev\" \"npm run -w client dev\"",
    "dev:vps": "concurrently -n SERVER,CLIENT -c auto \"npm run -w server dev\" \"npm run -w client dev -- --host 127.0.0.1 --port 5173 --strictPort\"",
    "build": "npm run -w client build",
    "start": "npm run -w server start",
    "migrate": "npm run -w server migrate",
    "studio": "npm run -w server studio"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
